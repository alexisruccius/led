defmodule LED.PatternTest do
  use ExUnit.Case

  #describe "%Pattern{} struct" do
  #  test "has default pattern programm list" do
  #    assert %Pattern{programm: progamm} = %Pattern{}
  #    assert [LED.intervals(100, 100), LED.blinking(100, 20)] = programm
  #  end
  #end
end
